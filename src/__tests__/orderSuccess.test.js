// import { mount } from "enzyme/build";
// import OrderPlaced from "../pages/ordered/OrderPlaced";
// import "../setupTests";

// const withChild = mount(<OrderPlaced />);

// describe("test if containers of order page are loaded", () => {
//   it("test if main container is loaded", () => {
//     expect(withChild.find(".order-container").exists()).toBe(true);
//   });
// });
